import 'package:flutter/material.dart';

class RestaurantAction extends StatelessWidget {
  const RestaurantAction({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
       title: const Text("Action"),
      ),
    );
  }
}
